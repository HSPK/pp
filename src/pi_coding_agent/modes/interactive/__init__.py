"""Interactive TUI mode.

Ported from `packages/coding-agent/src/modes/interactive/` in the TypeScript
"pi" monorepo: `interactive_mode.py` is the port of `interactive-mode.ts`,
alongside the theme system, the selector components and the extension
dialogs. See `interactive_mode.py`'s module docstring for the parts of that
file which are still missing.
"""
