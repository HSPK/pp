[project]
name = "pi-telemetry"
version = "0.0.1"
description = "Telemetry primitives (Python port of @earendil-works/pi-telemetry)"
requires-python = ">=3.11"
dependencies = [

]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pi_telemetry"]

[tool.uv.sources]

