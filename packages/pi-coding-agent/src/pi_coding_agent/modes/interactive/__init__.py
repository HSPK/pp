"""Interactive TUI mode.

Ported from `packages/coding-agent/src/modes/interactive/` in the TypeScript
"pi" monorepo. Only `modes.interactive.theme` is represented here; the
interactive TUI mode itself (`interactive-mode.ts`, 6399 lines) and its
selector components are not ported (see the top-level package README).
"""
