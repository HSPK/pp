[project]
name = "pp-monorepo"
version = "0.1.0"
description = "Python port of the pi monorepo (agent, ai, tui, coding-agent)"
requires-python = ">=3.11"
dependencies = [
    "pp-telemetry",
    "pp-ai",
    "pp-agent-core",
    "pp-tui",
    "pp-rpc-protocol",
    "pp-rpc-client",
    "pp-rpc-server",
    "pp-coding-agent",
    "pp-evals",
]

# The root project only exists to pull the workspace together for development.
# It must never be built or published; the nine `packages/*` members are.
[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
pp-telemetry = { workspace = true }
pp-ai = { workspace = true }
pp-agent-core = { workspace = true }
pp-tui = { workspace = true }
pp-rpc-protocol = { workspace = true }
pp-rpc-client = { workspace = true }
pp-rpc-server = { workspace = true }
pp-coding-agent = { workspace = true }
pp-evals = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "pytest-xdist>=3.6",
    "ruff>=0.8",
    "mypy>=1.13",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["packages"]
# `-n auto` matters: the suite is dominated by tests that wait (real
# `asyncio.sleep` for OAuth device-code polling, retry backoff, subprocess
# timeouts), so wall time was ~2.5x CPU time when run serially. Override
# with `-n0` to debug a single test.
addopts = "-q -n auto"
filterwarnings = ["error::DeprecationWarning:pi_.*"]

[tool.coverage.run]
branch = true
source = [
    "packages/pi-telemetry/src",
    "packages/pi-ai/src",
    "packages/pi-agent/src",
    "packages/pi-tui/src",
    "packages/pi-protocol/src",
    "packages/pi-client/src",
    "packages/pi-server/src",
    "packages/pi-coding-agent/src",
    "packages/pi-evals/src",
]

[tool.coverage.report]
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "@overload",
    "\\.\\.\\.$",
]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501", "B008", "SIM108", "RUF001", "RUF002", "RUF003"]

[tool.mypy]
python_version = "3.11"
warn_unused_ignores = true
ignore_missing_imports = true
